<?xml version="1.0" encoding="UTF-8"?>
<!--

    Robonews.io

    Copyright (c) 2013-2015 Rosty Kerei.
    All rights reserved.

-->
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task.xsd">

    <import resource="crawlerContext.xml"/>

    <task:scheduled-tasks scheduler="crawlScheduler">
        <task:scheduled ref="crawler" method="execute" fixed-rate="${workerCrawler.startInterval}"/>
    </task:scheduled-tasks>

    <task:scheduler id="crawlScheduler"/>

    <task:executor id="crawlExecutor"
                   pool-size="${workerCrawler.threadPoolSize}"
                   rejection-policy="DISCARD"
                   queue-capacity="${workerCrawler.threadQueueCapacity}"/>

    <task:annotation-driven scheduler="crawlScheduler" executor="crawlExecutor"/>


    <!-- Reset idle feeds each 5 minute-->
    <bean id="resetIdleFeedsJob" class="io.robonews.worker.crawler.job.ResetIdleFeeds">
        <constructor-arg name="rate" value="${workerCrawler.resetIdleFeedsRate}"/>
    </bean>

    <task:scheduler id="resetIdleFeedsScheduler"/>
    <task:scheduled-tasks scheduler="resetIdleFeedsScheduler">
        <task:scheduled
                ref="resetIdleFeedsJob"
                method="run"
                fixed-delay="${workerCrawler.resetIdleFeedsRate}"/>
    </task:scheduled-tasks>

</beans>